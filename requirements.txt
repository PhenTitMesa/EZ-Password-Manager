# Simple Password Manager - Dependencies
# Install with: pip install -r requirements.txt

# CustomTkinter - Modern UI library (replaces basic Tkinter)
customtkinter>=5.2.0

# Pyperclip - Cross-platform clipboard operations (copy/paste)
pyperclip>=1.8.2

# Note: Encryption uses Python's built-in modules (hashlib, hmac)
# No external cryptography library needed!
